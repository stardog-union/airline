package io.airlift.command.command;

import io.airlift.command.Command;

@Command(name = "start", description = "Start command without -v option")
public class CommandStart {

	public CommandStart() {
	}
}
